# PostgreSQL values used by Docker Compose and the backend defaults.
# Copy to .env only when you need to override the local defaults.
POSTGRES_DB=lurepilot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
